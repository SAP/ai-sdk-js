name: SAP AI SDK Support Bot

on:
  issues:
    types: [opened]
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to test with'
        required: true

permissions: {}

jobs:
  reply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # SEC-1: one run per issue at a time — prevents duplicate replies
    concurrency:
      group: issue-reply
      cancel-in-progress: false
    permissions:
      contents: read
      # issues: write

    steps:
      - uses: sap/cloud-sdk-js/.github/actions/setup@main

      - name: Generate answer
        id: answer
        timeout-minutes: 5
        working-directory: ./sample-code/src/support-bot
        env:
          AICORE_SERVICE_KEY: ${{ secrets.AI_CORE_PRODUCTION }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.inputs.issue_number || github.event.issue.number }}
        run: |
          node reply.ts "$ISSUE_NUMBER"

      - name: Build Slack payload
        env:
          ISSUE_URL: ${{ format('https://github.com/{0}/issues/{1}', github.repository, github.event.inputs.issue_number || github.event.issue.number) }}
        run: |
          jq -n \
            --arg url "$ISSUE_URL" \
            --rawfile answer /tmp/answer.md \
            '{
              "username": "SAP AI SDK Support Bot",
              "text": ("Support Issue: " + $url + "\n\n" + $answer)
            }' > /tmp/slack-payload.json

      - name: Notify Slack
        continue-on-error: true
        uses: slackapi/slack-github-action@dcb1066f776dd043e64d0e8ba94ca15cc7e1875d # v4.0.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_SUPPORT_BOT }}
          webhook-type: incoming-webhook
          payload-file-path: /tmp/slack-payload.json
