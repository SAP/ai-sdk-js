---
'@sap-ai-sdk/rpt': minor
---

[compat] The server default changed from for `parse_data_types` changed from `true` to `false`.
Pass `parse_data_types: true` explicitly if you relied on the old default.
