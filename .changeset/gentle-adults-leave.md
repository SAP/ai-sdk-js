---
'@sap-ai-sdk/document-grounding': minor
---

[compat] `GoogleDrivePipelineCreateRequest`: the `configuration` property is now required (was optional).
Code constructing this request without `configuration` must be updated to provide it.
