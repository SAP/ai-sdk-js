---
'@sap-ai-sdk/langchain': minor
---

[Compatibility Note] The `@langchain/core` package is now a peer dependency.
You now have to install the `@langchain/core` package as a direct dependency of your project.
