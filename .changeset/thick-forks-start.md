---
'@sap-ai-sdk/prompt-registry': minor
---

[feat] Update prompt-registry specification. Also, we can now create resource group scoped prompt templates if an orchestration configuration and deployment is present.
